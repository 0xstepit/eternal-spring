---
// Component Script (JavaScript)
interface Props {
  url: string;
  title: string;
  date: Date;
}

const { url, title, date } = Astro.props;
---

<li>
  <a href={url}>{title}</a>
  <p>{date}</p>
</li>
