---
import Social from "./Social.astro";
import { SOCIAL_LINKS } from "@/blogConfig";
---

<footer>
  {
    SOCIAL_LINKS.map((social) => {
      return <Social platform={social.platform} url={social.url} />;
    })
  }
</footer>
